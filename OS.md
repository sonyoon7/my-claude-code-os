# ATDD OS — 나만의 클로드 코드 OS 청사진

> 이 문서는 완결된 설계서가 아니라 **계속 자라나는 청사진**이다. 결론만 남기지 않고 왜 이렇게 정했는지, 아직 정하지 못한 것, 고민하다 접은 대안까지 함께 기록한다. 새로운 주차를 진행할 때마다 이 문서를 갱신한다.

## 0. 목표 (Goal)

> 이 섹션은 매주 갱신한다. 지금은 1주차 시작 시점의 초기 목표다.

- **현재 목표(1주차, 2026-08-26 기준)**: ATDD(인수 테스트 주도 개발) 방식을 Claude Code의 스킬·에이전트·훅·루프 개념으로 옮겨, "요구사항 → 인수기준 합의 → 실패하는 테스트 → 구현"이라는 흐름을 사람과 AI가 함께 돌릴 수 있는 최소 동작 체계를 만든다.
- **최종적으로 원하는 것**: 요구사항 한 줄을 던지면, AI가 인수기준을 제안하고, 사람이 승인한 뒤에는 AI가 실패하는 테스트부터 작성해 TDD로 구현하고, 그 과정의 실패 이력이 자동으로 남는 워크플로우.
- 이 목표는 아직 거칠다. 1주차 실습을 해보면서 범위가 더 구체화될 것이다.

## 1. 개요

- **이름(가칭)**: ATDD OS (스펙주도 OS)
- **한 줄 정의**: 요구사항이 들어오면, 사람과 AI가 합의한 인수기준(정책)을 통과할 때까지 실패하는 테스트로 검증하며 구현하는 체계.
- **골격 출처**: 온보딩.pdf 8페이지 "OS 예시 · 스펙·테스트 주도"의 파이프라인을 이 프로젝트에 맞게 구체화한 것.

## 2. 왜 ATDD인가

온보딩 자료가 정의하는 모든 OS의 공통 골격은 `사람+AI 의도 공유 → 사람+AI 실행 → 사람+AI 피드백 → 반복`이다. ATDD는 이 골격에 특히 잘 들어맞는다.

- **승인 게이트가 명확하다.** "인수기준(정책) 승인"이라는 지점에서 사람이 반드시 개입하게 되어, 강의가 계속 던지는 질문("AI에게 어디까지, 어떻게 맡겨야 하지?")에 대한 답을 설계 자체에 내장할 수 있다.
- **실패를 먼저 증명하는 구조**라서, AI가 "구현이 끝났다"고 주장하는 것과 "테스트가 실제로 통과했다"는 것을 구분할 수 있다. 이는 AI 결과물을 사람이 어떻게 검증할지에 대한 직접적인 연습이 된다.
- **Claude Code의 핵심 개념(스킬·에이전트·훅·루프)을 전부 자연스럽게 걸 수 있는 파이프라인**이라 4주 커리큘럼과 맞물려 매주 배운 개념을 얹기 좋다 (아래 4장 참고).

## 3. 전체 파이프라인

온보딩 8페이지 골격을 이 프로젝트 언어로 재정리:

| # | 단계 | 주체 | 설명 |
|---|------|------|------|
| 01 | 요구사항 입력 | 사람 | "이런 기능이 필요하다"를 자연어로 던진다 |
| 01.5 | (조건부) 요구사항 인터뷰 | AI ↔ 사람 | 6개 슬롯 중 3개 이상이 비면 질문으로 메우고 "인터뷰 브리프"로 동결한다. 2개 이하면 건너뛴다 |
| 02 | 정책(인수기준)으로 분해 후 멈춤 | AI | 요구사항을 검증 가능한 인수기준 목록으로 분해하고, 구현을 시작하지 않고 멈춘다 |
| 03 | 독립 리뷰 — PASS/FAIL | AI (별도 에이전트) | 분해를 수행한 에이전트와 다른 에이전트가 인수기준이 요구사항을 충분히 커버하는지 독립적으로 판정 |
| 04 | 정책 승인 | 사람 | 인수기준을 최종 승인 (게이트) |
| 05 | 실패하는 인수 테스트 먼저 작성 (Red) | AI | 승인된 인수기준을 실제 테스트 코드로 옮긴다. 이 시점엔 반드시 실패해야 한다 |
| 06 | TDD로 내부 구현 (Red-Green-Refactor) | AI | 인수 테스트를 통과시키기 위해 단위 수준에서 TDD 루프를 돌며 구현 |
| 07 | 실패 이력을 원장에 기록 | 자동 (훅) | 각 시도의 실패/재시도를 로그로 남겨 나중에 패턴을 분석할 수 있게 한다 |

**사람 vs AI 역할 분리** (온보딩 7~10페이지 형식):

- **사람 = 의사결정자**: 요구사항 제시(01), 인터뷰 응답(01.5), 정책 승인(04)만 담당
- **AI = 설계·구현자**: 나머지(02, 03, 05, 06, 07)를 수행하되, 02 이후 반드시 멈춰서 사람의 승인을 기다림

> **설계 노트**: 온보딩 7페이지 "티켓 처리" 예시는 실패 시 자동수정에 "3회 상한"을 둔다. ATDD OS의 06 단계(TDD 구현 루프)에도 동일하게 재시도 상한을 둘지는 아직 미정 — 1주차 실습 후 결정.

## 4. Claude Code 개념 매핑

| 파이프라인 단계 | Claude Code 구성요소 | 비고 |
|---|---|---|
| 02. 요구사항 → 정책 분해 | **스킬** (예: `.claude/skills/spec-decompose`) | 기존 `git-commit-message`, `skill-stat` 스킬과 같은 위치에 추가 예정 |
| 03. 독립 리뷰 PASS/FAIL | **별도 에이전트(subagent)** | 분해한 에이전트와 컨텍스트를 공유하지 않는 게 핵심 — 그래야 "독립적" 판정이 의미 있음 |
| 04. 정책 승인 게이트 | **사람의 명시적 확인** | AskUserQuestion 또는 PR 리뷰 방식 중 택1 (설계 노트 참고) |
| 05~06. 실패 테스트 → TDD 구현 | **루프/워크플로우** | Red-Green-Refactor를 반복하는 루프. 재시도 상한 포함 여부는 미정 |
| 07. 실패 원장 기록 | **훅(hook)** | 테스트 실행 결과 이벤트를 가로채 로그 파일에 기록. `skill-stat` 스킬이 이미 `.claude/skill-usage-stats.json`에 훅으로 통계를 기록하는 패턴을 그대로 참고 가능 |
| 02→05 정책 전달 | **컨텍스트 관리** | 승인된 인수기준을 다음 단계 에이전트에게 어떤 형식(파일? 메시지?)으로 넘길지 결정 필요 |

> **설계 노트**: `.claude/skills/skill-stat`이 이미 훅 기반 로깅의 실제 예시로 이 저장소에 있다. 07단계(실패 원장)를 구현할 때 이 스킬의 훅 등록 방식을 그대로 참고하면 새로 배울 것이 줄어든다.

## 5. MVP 범위 (1주차)

온보딩 13페이지 원칙("완성보다 개념을 실제로 연결해보는 것이 더 중요한 목표")에 따라 1주차는 전체 파이프라인을 다 만들지 않는다.

- **1주차에 만들 것**: 02단계(요구사항 → 정책 분해) 스킬 하나만 실제로 구현
- **1주차엔 흉내만 낼 것**: 03(독립 리뷰), 05~07(테스트/구현/기록)은 사람이 수동으로 수행하며 파이프라인 개념만 검증
- **업데이트(2026-08-26)**: 02단계 스킬에 더해, AI 간 협업(스킬→스킬, 스킬→서브에이전트) 구조를 직접 체득하기 위해 `atdd-orchestrator` 스킬과 `policy-reviewer` 서브에이전트의 **뼈대**도 함께 만들었다. 02~04단계(분해 → 독립 리뷰 → 사람 승인)까지는 실제로 동작하고, 05~07단계는 오케스트레이터 절차 안에 스텁으로만 존재한다(완성보다 개념 연결이 우선이라는 원칙 유지).
- **업데이트(2026-08-27) — 스텁 제거, 7단계 전부 동작**: 05~07단계를 실제 스킬(`acceptance-test-write`, `tdd-implement`, `failure-ledger`)로 구현하고 `atdd-orchestrator`의 스텁 문구를 실제 위임 호출로 교체했다. 여기에 공유 서브에이전트 `test-reviewer`, ATDD 전용 훅 `atdd-failure-log.js`, 현황 조회 스킬 `atdd-status`를 더해 **스킬 7개 + 서브에이전트 2개 + 훅 2개** 구성이 되었다. 1주차 MVP 범위를 넘어섰지만, 미션 요건("전체 사이클 1회 이상 구동")을 흉내가 아니라 실제로 만족시키려면 05~07이 진짜로 돌아야 했기 때문이다.

## 6. 향후 확장 (2~4주차 가이드 — 확정 아님)

1. **2주차**: 03단계 — 독립 리뷰 에이전트 추가
2. **3주차**: 05~06단계 — 실패 테스트 우선 작성 + TDD 구현 루프 자동화
3. **4주차**: 07단계 — 실패 원장 훅 + 전체 파이프라인 다듬기

> **참고(2026-08-26)**: 위 로드맵과 별개로, AI-to-AI 협업(스킬이 스킬을 부르고, 스킬이 서브에이전트를 부르는 구조)을 먼저 체득해보기 위해 03(독립 리뷰)·04(승인 게이트)의 **뼈대**를 1주차에 앞당겨 만들었다. 다만 이번 것은 `atdd-orchestrator`가 흉내 내는 최소 골격이며, 2주차 이후 계획된 정식 확장을 대체하지 않는다.

## 7. 열린 질문

- [x] 이 OS를 어떤 대상에 적용할 것인가? → **이 저장소(`my-claude-code-os`) 자체로 확정**(2026-08-26). 뼈대의 dry-run 요구사항도 이 저장소의 실제 다음 기능(예: skill-stat 기능 추가)으로 삼기로 함(9장 참고).
- [x] 정책 승인 게이트(04단계)를 AskUserQuestion 대화형으로 할지, PR 승인 방식으로 할지 → **AskUserQuestion으로 결정**(2026-08-26). `atdd-orchestrator` 스킬은 대화형으로 실행되므로 PR 리뷰 흐름이 자연스럽게 들어맞지 않고, AskUserQuestion을 쓰면 스킬 호출 안에서 바로 사람의 승인/반려를 받을 수 있어 더 적합하다고 판단.
- [x] TDD 구현 루프(06단계)에 재시도 상한을 둘지, 두면 몇 회로 할지 → **3회로 확정**(2026-08-27). 온보딩 7페이지 "티켓 처리" 예시의 3회 상한을 그대로 따랐다. 함께 정한 것: **04→02 재분해에는 상한을 두지 않는다.** 스펙을 고치는 논의는 값싸고 정확도를 올리지만, 구현 삽질은 비싸고 되풀이할수록 나빠지기 때문이다. 상한 초과 시 AI가 스스로 더 시도하지 않고 사람에게 인계한다.
- [ ] 승인된 인수기준을 세션 사이에 어디에 저장할 것인가 — 지금은 대화 컨텍스트에만 있어서 세션이 끊기면 04단계 승인 결과가 사라진다(9장 "상태 관리" 항목)
- [ ] `lastUsedAt` 비교를 문자열 사전순이 아니라 실제 시각으로 해야 한다는 리뷰어 지적을 AC-9로 추가할지 — 이번 사이클에서는 **승인 범위 밖이라 일부러 넣지 않았다.** 승인 게이트를 우회해 슬쩍 넣는 순간 04단계가 무의미해지기 때문. 다음 사이클의 요구사항으로 올린다.
- [x] `ui-visual-reviewer`가 스크린샷을 찍는 데 쓰는 `claude-in-chrome`이 '제로 디펜던시' 철학과 어떤 관계인지 → **실제로 걸림을 확인**(2026-08-28). 확장 미설치 상태에서 dry-run을 돌렸더니 자동 스크린샷이 막혔고, 사람이 브라우저로 직접 열어 스크린샷을 제공하는 수동 대안으로 우회했다. 즉 `claude-in-chrome`은 "있으면 자동화, 없으면 사람이 대신하는" 선택적 의존으로 남겨둔다 — 06단계 절차에 하드 요구사항으로 넣지 않는다.
- *(참고, 설계 대상 아님, 2026-09-02)* Confluence/Slack 연동은 이번 범위에서 제외한다. 나중에 붙일 때는 세션 시작 시 미리 캐싱하는 방식보다 **필요할 때 MCP로 조회하는 방식**을 권장 — 이번 세션에 이미 연결된 notion MCP가 그 이음새(seam)를 그대로 보여준다.

## 8. 설계 노트 (Design Notes)

시간순으로 누적. 최종 결론이 아니라 그 순간의 고민을 남긴다.

- **2026-08-26**: 강사 추천으로 ATDD를 주제로 확정. 온보딩 8페이지 "스펙·테스트 주도" 예시를 골격으로 채택하기로 함 — 이미 검증된 패턴이라 처음부터 새로 설계하는 것보다 리스크가 적다고 판단.
- **2026-08-26**: 오케스트레이터 뼈대(스킬이 스킬을 부르고, 스킬이 서브에이전트를 부르는 구조)를 지금 그냥 만들면, 실제로 흘려볼 요구사항이 없어 "잘 작동하는지" 검증할 수 없다는 걸 깨달음 → 9장의 기능 로드맵을 정리하면서, 뼈대가 완성되면 **이 저장소 자체를 대상으로 한 실제 요구사항 1개**로 dry-run 검증하기로 함(7장 열린 질문 1번 답과 연결).
- **2026-08-26**: `atdd-orchestrator`(`.claude/skills/atdd-orchestrator/SKILL.md`), `spec-decompose`(`.claude/skills/spec-decompose/SKILL.md`), `policy-reviewer`(`.claude/agents/policy-reviewer.md`) 3개 파일을 실제로 생성함. 이 과정에서 `.claude/agents/`라는 이 저장소 최초의 서브에이전트 정의 위치를 새로 도입함 — 지금까지는 스킬만 있었고 커스텀 서브에이전트 파일은 없었다. 02~04단계는 실제로 동작하고, 05~07단계는 오케스트레이터 절차 안에 스텁 문구로만 남겨둠. 다음으로 할 일: 9장의 dry-run 요구사항으로 실제 실행해 검증.

- **2026-08-27 — dry-run에서 배운 것: 독립 리뷰가 실제로 일을 했다.** 뼈대를 `"skill-stat에 '가장 최근 사용한 스킬' 표시 기능 추가"` 요구사항으로 돌려봤다. 1차 분해(AC-1~7)에 대해 `policy-reviewer`가 **FAIL**을 냈고, 지적 내용이 실제로 옳았다 — (a) 유효한 `lastUsedAt`이 *하나도 없는* 경우가 미정의, (b) AC-2가 "규칙 예시 + 감추지 말 것"으로 서로 충돌해 합격/불합격을 가를 수 없는 형태. 재분해(AC-1~8) 후 2차 리뷰에서 PASS. **컨텍스트 차단이 형식적 절차가 아니라 실제로 다른 판단을 만든다**는 걸 확인한 게 이번 실습의 가장 큰 수확이다.
- **2026-08-27 — 마크다운은 테스트할 수 없다는 벽.** 05단계를 진짜로 돌리려니 이 저장소에 검증 가능한 대상이 없었다(전부 스킬 지시문 마크다운). 그래서 `skill-stat`의 집계 로직을 `.claude/lib/stats.js`의 순수 함수(`readStats`/`summarize`)로 분리하고 `.claude/tests/stats.test.js`에서 검증하기로 했다. **"AI에게 시킬 일을 검증 가능하게 만들려면 먼저 검증 가능한 표면을 만들어야 한다"**는 게 이 결정의 교훈. 앞으로 새 기능은 (로직=js) + (지시문=md)로 나눠 만든다.
- **2026-08-27 — 훅으로 규율을 강제한 이유.** 07단계를 스킬로만 두면 AI가 바쁠 때 "원장 기록"을 빼먹는다. `atdd-failure-log.js`를 `PostToolUse`/`Bash` 매처로 걸어 `node --test` 출력을 가로채게 하니 **AI가 깜빡해도 기록이 남는다.** 규율을 성실성이 아니라 시스템에 맡기는 설계 — 이게 훅을 쓰는 가장 좋은 이유라고 판단.
- **2026-08-27 — 실습 중 만난 두 가지 함정(기록해 둘 것)**: ① Node 테스트 러너는 `.`으로 시작하는 **숨김 디렉터리를 자동 탐색에서 제외**한다. `node --test .claude/tests/`는 실패하고 파일을 직접 지정해야 한다. ② **커스텀 서브에이전트 정의는 세션 시작 시점에 로드**되므로, 방금 만든 `.claude/agents/*.md`는 그 턴에는 호출할 수 없다(`Agent type 'test-reviewer' not found`). 실제로 `test-reviewer`를 만든 턴에는 호출이 막혀 정의를 그대로 주입한 임시 대역으로 05·06단계 리뷰를 돌렸고, **다음 턴에 정식 등록되는 것을 확인**했다. 새 서브에이전트를 만들었다면 그 턴에는 쓸 수 없다고 보고 계획을 짜는 게 맞다.
- **2026-08-27 — 다이어그램을 Mermaid 코드블록에서 SVG 이미지로 바꿈.** 10장을 처음 쓸 때는 `OS.md`에 Mermaid 코드블록을 그대로 넣었는데 가독성이 나빴다. 특히 7단계를 한 장에 세로로 그리니 630×1685의 극단적인 비율이 나왔다. 세 가지를 바꿨다: ① 파이프라인을 **04 승인 게이트 기준으로 두 장**으로 분리(앞은 루프 상한 없음, 뒤는 3회 상한 — 성격이 다르므로 나누는 게 개념적으로도 맞다), ② `@mermaid-js/mermaid-cli`로 SVG를 미리 생성해 `docs/diagrams/`에 두고 이미지로 참조, ③ **`htmlLabels: false`** 설정 — 기본값이면 라벨이 `<foreignObject>`로 들어가는데 GitHub이 마크다운 이미지를 `<img>`로 렌더링할 때 그 안의 HTML은 실행되지 않아 글자가 통째로 사라진다. 소스(`.mmd`)를 원본으로 남겨 두어 나중에도 다시 뽑을 수 있게 했다.
- **2026-08-28 — 회고를 강제하는 두 번째 훅.** 07단계 실패 원장(`atdd-failure-log.js`)이 "기록"을 훅에 맡겼던 것처럼, OS.md 갱신도 사람이 매번 기억해서 하면 빼먹는다. `Stop` 이벤트에 `os-retro-check.js`를 걸어, `.claude/skills|agents|hooks|lib|tests/`나 `docs/`가 바뀐 세션 끝에서만 발동하게 했다. 다만 Stop 훅은 명령만 실행할 뿐 사람에게 직접 물을 수 없어서, `decision:"block"`으로 턴을 이어가게 한 뒤 AI가 회고를 정리하고 AskUserQuestion으로 반드시 먼저 물어본 뒤에만 OS.md를 고치도록 지시를 되돌려주는 구조로 만들었다 — "규율은 훅, 판단과 대화는 AI"라는 원칙을 기록·확인 두 훅에서 그대로 반복한 셈이다.
- **2026-08-28 — 커밋 규율도 훅으로.** 회고 확인(`os-retro-check.js`)과 실패 기록(`atdd-failure-log.js`)에 이어, 세 번째로 같은 패턴을 반복했다: `big-change-commit-check.js`를 `Stop` 이벤트에 걸어, 커밋 안 된 변경이 파일 5개·줄 150줄 이상으로 커지면 AI가 `AskUserQuestion`으로 먼저 사람에게 중간 커밋 여부를 물은 뒤에만 커밋하게 만들었다. 만들면서 실제로 겪은 함정: 이 훅의 중복 방지 상태 파일(`.commit-check-state.json`) 자신이 `git status`에 잡히면 서명이 매번 달라져 무한 재발동하는 버그가 있었는데, `os-retro-check.js`의 상태 파일이 이미 `.gitignore`에 등록되어 있던 것을 보고 동일하게 처리해 해결했다.
- **2026-08-28 — 첫 UI dry-run: 이미지→구현→시각검증까지 끝까지 돌았다.** 인쇄된 스도쿠 퍼즐 사진 한 장으로 `atdd-orchestrator`를 처음부터 끝까지 돌려 "수도쿠 게임 만들기"를 구현했다. 확인된 것 세 가지: ① **독립 리뷰어의 컨텍스트 차단이 반복해서 같은 종류의 마찰을 낳았다** — `policy-reviewer`는 "퍼즐 생성 유효성"을, `ui-visual-reviewer`는 "셀 숫자가 사진과 다르다"를 각각 FAIL로 지적했는데, 둘 다 사람이 이미 내린 스코프 결정(퍼즐 값 자체는 검증 대상 밖)을 리뷰어가 모르기 때문에 생긴 마찰이었다. policy-reviewer에게는 "의도적으로 제외했다"는 메모까지 줬는데도 리뷰어가 그 메모를 무시하고 같은 판정을 냈다 — 컨텍스트 차단이 형식이 아니라 실제로 작동한다는 증거이자, 04(사람 승인) 게이트가 왜 필요한지 다시 확인한 지점이다. ② `dom-lite.js`가 실제 UI 12개 AC를 한 번의 시행착오(테스트 자체의 삼항 폴백 결함 1건 수정) 만에 전부 통과시켰다 — jsdom 없이도 충분했다. ③ `claude-in-chrome` 확장이 설치되어 있지 않아 자동 스크린샷이 막혔고, 사람이 브라우저로 직접 열어 스크린샷을 제공하는 수동 대안으로 우회했다 — 7장에 미리 남겨둔 "제로 디펜던시와 런타임 도구 의존의 긴장"이 실제로 발생한 사례다.
- **2026-08-28 — UI 요구사항을 위한 이미지 스펙·구현·시각검증 확장.** 지금까지 파이프라인이 순수 로직만 다뤄 UI 요구사항을 못 담았다. `ui-spec-from-image` 스킬(02단계 앞, 이미지→UI 스펙 문서)과 `ui-visual-reviewer` 서브에이전트(06단계, 원본 목업·스크린샷 두 장만 보고 시각 일치 판정)를 새로 만들고, 기존 5개 스킬/에이전트(`atdd-orchestrator`·`spec-decompose`·`policy-reviewer`·`acceptance-test-write`·`tdd-implement`)의 절차를 확장했다. 트레이드오프 세 가지: ① jsdom 대신 자체 경량 파서 `.claude/lib/dom-lite.js`를 만들어 '제로 디펜던시' 철학을 지켰다(정규식 기반이라 완전한 파서는 아님). ② `policy-reviewer`에 UI 스펙 문서뿐 아니라 원본 이미지도 함께 줌 — 스펙 문서만 주면 이미지 오독을 같은 오독으로만 재검증하는 순환 검증이 되기 때문. ③ 06단계 3회 재시도 상한을 구조 테스트와 시각 비교가 공유하게 했다 — 상한을 이원화하면 '구현 삽질은 비싸다'는 기존 원칙이 사실상 6회로 완화되기 때문. 아직 실제 이미지로 dry-run은 하지 않았다(다음 세션 과제).
- **2026-09-02 — 01.5단계 `requirement-interview` 스킬 추가.** 파이프라인의 01단계가 "사람이 요구사항을 던진다"로만 되어 있어, 모호한 요구사항이 들어오면 `spec-decompose`가 *가정을 만들어* 메우고 있었다. 외부 사례 3종(obra/superpowers `brainstorming`, garrytan/gstack `/spec`, Q00/ouroboros `interview`)의 실제 소스를 읽고 비교한 뒤, 절차(질문 형식·코드 먼저 읽기·분류별 예산·승인 전 구현 금지)는 그대로 차용하고 **접합부는 새로 정했다**. 세 사례 모두 자기 파이프라인 안에서 끝나기 때문에 기존 7단계에 끼워 넣는 규칙은 베낄 대상이 없었고, 실제로 정책 구멍도 전부 거기서 나왔다. 결정 다섯 가지: ① **진입 판정을 감각이 아니라 슬롯 결손 수(6개 중 3개 이상)로** 정의 — "모호해 보인다"로는 매번 기준이 달라진다. ② **탈출구("인터뷰 건너뛰고 진행")를 항상 제공** — 빠져나갈 구멍이 없는 게이트는 우회당하고 우회는 기록이 안 남는다. ③ **브리프는 동결 스냅샷**, 04단계에서 AC가 바뀌어도 갱신하지 않는다 — 두 문서를 동기화하는 정책은 반드시 깨지므로 진실 원천을 AC 한쪽으로 몰았다. ④ **가정을 금지하는 대신 가정이 적히는 자리를 브리프 한 곳으로 고정** — `spec-decompose`는 이제 스스로 가정을 만들지 않고 브리프의 `미해결 가정`만 인용한다. ⑤ **ouroboros식 모호성 점수(0.2 게이트)는 도입하지 않았다** — 이미 04단계 사람 승인과 `policy-reviewer`가 있어 세 번째 게이트는 중복이고, 그 게이트가 LLM 주관 점수라면 셋 중 신뢰도가 가장 낮다. 이미지가 첨부된 경우 `ui-spec-from-image`를 인터뷰보다 **앞으로** 옮겼다(이미지가 이미 답한 것을 다시 묻지 않기 위해). 검증의 한계는 코드 주석과 스킬 본문 양쪽에 명시했다: `interview-brief.js`(AC-1~11)가 보는 것은 형식과 완결성뿐이고, 슬롯에 그럴듯한 문장을 넣으면 통과한다 — "테스트 초록불 = 인터뷰가 잘 됐다"는 착각이 이 스킬의 가장 위험한 실패 모드다. 아직 실제 요구사항으로 dry-run은 하지 않았다(다음 세션 과제).
- **2026-09-02 — 개인 컨텍스트 계층과 `context-map` 스킬을 추가함.** 사용자가 "나만의 지침을 컨텍스트로 주입하고 싶다"와 "세션이 얼마나 컨텍스트를 만드는지 구조적으로 보고 싶다"를 물어와, ATDD 파이프라인이 아니라 OS 전체의 컨텍스트 로딩 구조를 다루는 첫 기능이 됐다. 결정 세 가지:
  ① **`CLAUDE.md` → `.claude/context/index.md`(레지스트리) → 실제 지침 파일**로 한 단계를 더 두었다. `CLAUDE.md`가 지침 파일을 직접 나열하면 지침을 추가할 때마다 저장소의 "얼굴"인 루트 파일을 계속 건드리게 되는데, `atdd-status`가 `failure-ledger`의 데이터를 직접 나열하지 않고 안내만 하는 것과 같은 "얇은 인터페이스" 습관을 재사용했다.
  ② `docs/diagrams/06-context-map.mmd`는 스킬을 실행할 때마다 다시 그리지 않는 **정적** 다이어그램으로 뒀다. 다섯 계층이 "이런 종류로 나뉜다"는 분류 자체는 자주 안 바뀌는데, `context-map` 스킬의 텍스트 보고서가 이미 "지금 이 순간의 수치"를 담당하기 때문이다.
  ③ 새 훅은 **의도적으로 만들지 않았다.** `context-map`이 읽는 것(CLAUDE.md, 스킬/에이전트 파일, settings.json)은 전부 디스크에 이미 있는 정적 소스이지, 사람/AI가 바쁠 때 깜빡할 수 있는 누적 로그가 아니다. "훅이 쓰고 스킬이 읽는다"는 원칙은 후자에만 필요하다.
  구현 중 실제로 겪은 함정 하나: `index.md`에 "이렇게 추가하세요"라는 예시를 마크다운 코드블록(\`\`\`) 안에 `@경로` 형태로 적었더니, `parseImportLines`가 코드블록을 구분하지 못해 그 예시 줄을 **진짜 import로 인식**해 존재하지 않는 파일을 `missing`으로 보고했다 — `context-map`을 실제 저장소에 돌려보고서야 발견했다. 코드블록 인식 로직을 추가하는 대신, 예시 자체를 줄 맨 앞에 `@`가 오지 않는 문장 속 인라인 코드로 바꿔 근본 원인을 없앴다. **파서를 더 똑똑하게 만들기보다, 파서가 오해할 수 없는 형태로 입력을 쓰는 쪽이 더 단순하고 안전할 때가 있다.**

## 9. 디벨롭에 필요한 기능 정리

지금은 아무것도 없는 상태라, ATDD OS를 계속 키워나가려면 파이프라인 단계별로 어떤 기능이 필요한지 미리 정리해둔다. 상태는 매주 갱신한다.

| 단계 | 상태 | 필요한 기능 |
|---|---|---|
| 01.5 요구사항 인터뷰 | ✅ 동작 (2026-09-02 추가, 실제 dry-run은 미실시) | `requirement-interview` 스킬 — 슬롯 6개 중 3개 이상 결손일 때만 진입, 분류별 질문 예산(spike 1~2 / bounded 3~5 / architectural 최대 8), 산출물은 동결되는 인터뷰 브리프. 형식·완결성은 `.claude/lib/interview-brief.js` + AC-1~11로 검증하되 **인터뷰 품질은 검증하지 않는다** |
| 02 정책 분해 | ✅ 동작 | `spec-decompose` 스킬 — 요구사항(또는 인터뷰 브리프)을 인수기준 목록으로 분해. 스스로 가정을 만들지 않고 브리프의 `미해결 가정`만 인용한다 |
| 03 독립 리뷰 | ✅ 동작 | `policy-reviewer` 서브에이전트 — 분해 과정과 컨텍스트를 공유하지 않고 PASS/FAIL 판정 |
| 04 승인 게이트 | ✅ 동작 | AskUserQuestion 기반 사람 승인 확인 (7장 열린 질문에서 확정) |
| — 오케스트레이션 | ✅ 동작 | 위 스킬·서브에이전트를 순서대로 호출·중개하는 `atdd-orchestrator` 스킬 |
| 05 실패 테스트 작성 | ✅ 동작 | `acceptance-test-write` 스킬 — 승인된 AC를 `node:test` 코드로 1:1 변환하고 **반드시 실패하는 것을 확인**한 뒤 멈춤. 테스트 러너는 **Node 내장 `node:test`로 확정**(의존성 설치 불필요) |
| 06 TDD 구현 루프 | ✅ 동작 | `tdd-implement` 스킬 — 최소 구현 + 재시도 **상한 3회**, 초과 시 사람에게 인계. 테스트 코드 수정·완화는 금지 |
| 07 실패 원장 | ✅ 동작 | `atdd-failure-log.js` 훅이 `node --test` 출력을 파싱해 `.claude/atdd-failure-ledger.json`에 자동 기록, `failure-ledger` 스킬이 읽어서 반복 실패 분석 |
| — 공유 서브에이전트 | ✅ 동작 | `test-reviewer` — 05(`TEST_QUALITY`)와 06(`IMPL_HONESTY`)에서 모드만 바꿔 재사용. `policy-reviewer`도 `atdd-orchestrator`·`spec-decompose` 양쪽에서 공유 |
| — (조건부) UI 스펙 추출 | ✅ 동작 (2026-08-28 첫 dry-run 완료, 사람 오버라이드 2회 포함) | `ui-spec-from-image` 스킬 — 이미지가 첨부된 경우 02단계 전에 위임되어 레이아웃/컴포넌트/텍스트/색상을 `ui-spec.md`로 정리 |
| — (조건부) UI 구조·시각 검증 | ✅ 동작 (2026-08-28 첫 dry-run 완료, 사람 오버라이드 2회 포함) | `dom-lite.js`(경량 HTML/CSS 파서, jsdom 미도입, 12개 AC로 자체 검증 완료)로 05단계 구조 테스트, `ui-visual-reviewer` 서브에이전트로 06단계 스크린샷 vs 원본 목업 시각 비교. 3회 재시도 상한은 구조 테스트와 공유 |
| 상태 관리 | ⏳ 미정 | 지금 파이프라인이 몇 단계에 있는지, 승인된 정책이 무엇인지 저장할 곳 — 없으면 오케스트레이터가 매번 처음부터 다시 설명해야 함. **세션이 끊기면 승인 결과가 사라지는 문제가 실제로 확인됨** |
| 결과 가시화 | ✅ 동작 | `atdd-status` 스킬 — 구성요소·단계별 동작 여부·현재 초록불/빨간불을 한 화면으로. 호출 횟수는 `skill-stat`, 실패 이력은 `failure-ledger`가 분담 |
| 개인 가이드라인 계층 | 🔧 뼈대만 | `.claude/context/` — `CLAUDE.md`가 `index.md`를 `@import`하는 구조는 동작하지만, 실제 개인 지침 내용은 아직 하나도 없음(2026-09-02) |
| 컨텍스트 지도 | ✅ 동작 | `context-map` 스킬 — 세션 시작 시 항상 로드되는 것(CLAUDE.md 계열, 스킬/에이전트 요약)과 온디맨드로만 로드되는 것(스킬·에이전트 본문), 컨텍스트 비용이 0인 훅을 계층별로 보여줌. ATDD 파이프라인 밖 |

> **설계 노트**: "적용 대상"을 이 저장소 자체로 정하면, 뼈대의 첫 dry-run 요구사항도 자연스럽게 "이 저장소의 다음 기능"으로 정해진다 — 예) *"skill-stat 스킬에 '가장 최근 사용한 스킬' 항목도 보여주는 기능 추가"*. 이렇게 하면 05~07단계(실패 테스트 → TDD 구현)로 이어갈 실질적인 대상도 자연히 생긴다.

## 10. OS 구조와 오케스트레이션 (2026-08-27 기준)

### 10.1 요약

**구성(2026-09-02 기준 재집계, `context-map` 스킬로 확인)**: 스킬 11개 + 서브에이전트 3개 + 훅 4개. 이 중 ATDD 파이프라인에 직접 참여하는 것은 스킬 8개(`ui-spec-from-image`·`requirement-interview` 포함)·서브에이전트 3개(`ui-visual-reviewer` 포함)·훅 1개이고, 나머지(`skill-stat`, `git-commit-message`, `context-map`, `log-skill-usage.js`, `os-retro-check.js`, `big-change-commit-check.js`)는 파이프라인 밖의 보조 도구다. (이전 판(2026-08-27) 기록에 남아 있던 "스킬 8개+서브에이전트 2개"는 이후 `ui-spec-from-image`/`ui-visual-reviewer`가 추가되며 이미 낡아 있었다 — 상태 표시 스킬로 셀 때마다 재확인하는 습관을 들인다.)

**한 문장 정의**: 요구사항 한 줄을 받아 `atdd-orchestrator`가 7단계를 지휘하되, **자신은 아무 일도 직접 하지 않고** 각 단계를 전담 스킬과 서브에이전트에 위임하는 구조.

**이 구조를 지탱하는 규칙 4가지**

| # | 규칙 | 왜 |
|---|---|---|
| 1 | 지휘자는 어떤 단계도 직접 수행하지 않는다 | 지휘자가 "이건 간단하니 내가 하지"를 시작하는 순간 위임 구조가 무너지고, 리뷰어에게 넘길 컨텍스트도 오염된다 |
| 2 | 리뷰어에게는 판단의 **입력**만 주고 **과정**은 주지 않는다 | 분해한 쪽의 논리를 같이 주면 리뷰어가 거기에 끌려간다. 실제로 이 규칙 덕분에 1차 리뷰가 FAIL을 냈다 |
| 3 | 판정 전문성은 한 에이전트에 모으고, 호출 문맥(모드)만 바꾼다 | 스킬마다 리뷰어를 따로 두면 판정 기준이 갈라진다 |
| 4 | 기록은 사람·AI가 아니라 훅이 한다 | AI가 기억해서 기록하면 바쁠 때 빼먹는다. 훅이면 깜빡해도 남는다 |

### 10.2 파일 지도

```
.claude/
├── agents/                      서브에이전트 3개 — 판정만 하고 코드는 고치지 않음
│   ├── policy-reviewer.md         인수기준이 요구사항을 커버하는가        (03단계) ★공유
│   ├── test-reviewer.md           테스트/구현이 정직한가                 (05·06단계) ★공유
│   └── ui-visual-reviewer.md      목업 이미지 vs 스크린샷 시각 일치       (06단계 UI 게이트)
├── skills/                      스킬 11개
│   ├── atdd-orchestrator/         7단계 전체를 지휘 (아무것도 직접 하지 않음)
│   ├── requirement-interview/     01.5 모호한 요구사항 → 인터뷰 브리프 (동결), 그리고 멈춤
│   ├── spec-decompose/            02 요구사항/브리프 → 인수기준, 그리고 멈춤
│   ├── ui-spec-from-image/        (조건부) 01.5 앞, 목업 이미지 → UI 스펙 문서
│   ├── acceptance-test-write/     05 실패하는 인수 테스트 (RED 확인 필수)
│   ├── tdd-implement/             06 최소 구현 (재시도 상한 3회)
│   ├── failure-ledger/            07 실패 원장 조회·반복 실패 분석
│   ├── atdd-status/               ATDD 파이프라인 현황 한 화면 (조회 전용)
│   ├── context-map/               세션 시작 시 로드되는 컨텍스트 지도 (조회 전용) — 파이프라인 밖
│   ├── skill-stat/                스킬 호출 통계          — 파이프라인 밖
│   └── git-commit-message/        커밋 메시지 초안        — 파이프라인 밖
├── context/                     개인 업무 가이드라인 계층 (CLAUDE.md가 @import)
│   ├── README.md                  컨벤션 설명
│   └── index.md                   실제 지침 파일 레지스트리 (아직 뼈대만, 2026-09-02)
├── hooks/                       훅 4개 — 모두 best-effort(실패해도 작업을 막지 않음)
│   ├── log-skill-usage.js         PostToolUse/Skill → skill-usage-stats.json
│   ├── atdd-failure-log.js        PostToolUse/Bash  → atdd-failure-ledger.json  (node --test 출력만 파싱)
│   ├── os-retro-check.js          Stop              → OS.md 반영 여부를 AI가 되묻게 함 (직접 쓰지 않음, .os-retro-state.json으로 중복 알림 방지)
│   └── big-change-commit-check.js Stop              → 커밋 안 된 변경이 크면 AI가 커밋 여부를 되묻게 함 (직접 커밋하지 않음, .commit-check-state.json으로 중복 알림 방지)
├── lib/
│   ├── interview-brief.js         검증 가능한 로직 (requirement-interview용) — 브리프 슬롯 결손·플레이스홀더·범위밖 잠금 검사
│   ├── stats.js                   검증 가능한 로직 (skill-stat용)
│   └── context-map.js             검증 가능한 로직 (context-map용) — @import 트리 해석, 스킬/에이전트/훅 집계
└── tests/
    ├── stats.test.js              AC-1~8 인수 테스트
    └── context-map.test.js        AC-1~10 인수 테스트
```

> **설계 노트**: `lib/`과 `tests/`가 생긴 이유는 05단계 때문이다. 스킬은 마크다운 지시문이라 자동 테스트가 불가능하다. 그래서 **검증할 것은 `lib/`의 코드로, 보여줄 것은 `skills/`의 지시문으로** 나눈다. 앞으로 새 기능도 이 분리를 따른다.

### 10.3 전체 파이프라인

되돌아가는 화살표(FAIL → 이전 단계)가 이 파이프라인의 핵심이다. 직선으로 흐르기만 한다면 리뷰 단계를 둘 이유가 없다.

파이프라인은 **04 승인 게이트를 기준으로 성격이 갈린다.** 앞은 사람과 AI가 스펙에 합의하는 구간이라 되돌아가는 데 상한이 없고, 뒤는 코드를 만지는 구간이라 재시도에 상한이 걸린다. 그래서 두 장으로 나눠 그렸다.

**① 스펙 합의 구간 (01~04) — 승인 이전, 루프 상한 없음**

![스펙 합의 구간 01~04](docs/diagrams/01-pipeline-spec.svg)

**② 구현 구간 (05~07) — 승인 이후, 06단계만 3회 상한**

![구현 구간 05~07](docs/diagrams/02-pipeline-build.svg)

> 색 규칙(모든 다이어그램 공통): 🟡 사람 · 🟢 스킬 · 🔵 서브에이전트 · 🔴 훅 · ⚪ 데이터 파일·시작/종료

### 10.4 오케스트레이션 — 실제 호출 순서

**스킬이 스킬을 부르고, 그 스킬이 다시 서브에이전트를 부르는 2단 위임**이 05·06단계에서 일어난다.

![오케스트레이션 호출 순서](docs/diagrams/03-orchestration.svg)

세로줄 8개가 각각 하나의 참여자다. `test-reviewer` 줄에 화살표가 **05·06 두 곳에서** 들어오는 것이 공유 서브에이전트가 실제로 재사용되는 지점이다.

### 10.5 공유 서브에이전트

미션 요건이기도 하지만, 그보다 **판정 기준을 한 곳에 모으기 위한** 설계다. 스킬마다 리뷰어를 따로 두면 같은 질문에 다른 잣대가 적용된다.

![공유 서브에이전트 재사용 관계](docs/diagrams/04-shared-agents.svg)

| 서브에이전트 | 공유하는 스킬 | 판정하는 것 | 도구 |
|---|---|---|---|
| `policy-reviewer` | `atdd-orchestrator`, `spec-decompose` | 인수기준이 요구사항을 커버하는가 | `Read` |
| `test-reviewer` | `acceptance-test-write`, `tdd-implement` | 테스트/구현이 정직한가 | `Read` |

두 에이전트 모두 **코드를 고치지 않는다.** 무엇이 문제인지만 짚고, 수정은 호출한 스킬이나 사람의 몫이다. 판정자와 수정자를 분리해야 "통과시키려고 기준을 낮추는" 일이 생기지 않는다.

### 10.6 데이터 흐름 — 훅이 쓰고, 스킬이 읽는다

기록하는 쪽과 보여주는 쪽을 완전히 분리했다. 조회 스킬은 **어떤 경우에도 데이터 파일에 쓰지 않는다.**

![데이터 흐름](docs/diagrams/05-data-flow.svg)

화살표가 **한 방향으로만 흐른다**는 점이 중요하다. 훅(빨강)만 데이터 파일(회색)에 쓰고, 스킬(초록)은 읽기만 한다. 두 데이터 파일 모두 로컬 실행 데이터이므로 `.gitignore`에 넣었다.

> **다이어그램 원본**: 위 5개 그림의 Mermaid 소스와 재생성 방법은 [`docs/diagrams/`](docs/diagrams/)에 있다. 그림을 고칠 때는 SVG가 아니라 `.mmd`를 고치고 다시 생성한다.

### 10.7 되돌아가는 루프와 상한

| 루프 | 상한 | 근거 |
|---|---|---|
| 03 FAIL → 02 재분해 | **없음** | 스펙을 고치는 논의는 값싸고 정확도를 올린다. 실제로 1회 되돌아가서 AC-8이 추가됐다 |
| 04 "수정 필요" → 02 재분해 | **없음** | 위와 같음 — 사람이 납득할 때까지 돌아도 된다 |
| 05 리뷰 FAIL → 테스트 재작성 | 없음 | 아직 코드를 쓰기 전이라 값싸다 |
| 06 구현 재시도 | **3회** | 온보딩 7페이지 "티켓 처리" 예시의 3회 상한. 구현 삽질은 비싸고 되풀이할수록 나빠진다 |

**상한 초과 시 AI는 스스로 더 시도하지 않고 사람에게 인계한다.** 무한히 시도하는 AI보다 빨리 손드는 AI가 낫다는 판단이다. 인계 시에는 "구현 문제인지, 인수기준이 모순되어 02로 돌아가야 하는지"에 대한 판단을 함께 보고한다.

### 10.8 첫 사이클 실주행 기록 (2026-08-27)

요구사항: *"skill-stat 스킬에 '가장 최근에 사용한 스킬' 항목도 함께 보여주는 기능을 추가한다"*

| 단계 | 결과 |
|---|---|
| 02 분해 | AC-1~7 |
| 03 리뷰 | **FAIL** — 유효한 `lastUsedAt`이 하나도 없는 경우 미정의, AC-2가 판정 불가능한 형태 |
| 02 재분해 | AC-1~8 (AC-2 재작성 + AC-8 신설) |
| 03 재리뷰 | **PASS** |
| 04 승인 | 사람이 승인 |
| 05 테스트 | 8건 작성 → RED 확인 → `test-reviewer` PASS |
| 06 구현 | 1회차/3회 만에 `8 pass / 0 fail` → `test-reviewer` PASS |
| 07 원장 | FAIL → FAIL → RESOLVED 자동 기록, 🟢 |

산출물: `.claude/lib/stats.js`, `.claude/tests/stats.test.js`, 갱신된 `skill-stat` 스킬.

> **설계 노트**: 이 표에서 가장 중요한 줄은 **03 FAIL**이다. 리뷰어는 분해 과정을 전혀 모르는 상태에서 실제로 존재하는 구멍 두 개를 짚었다. 컨텍스트 차단이 형식적 절차가 아니라 실제로 다른 판단을 만든다는 것을 확인한 것이, 뼈대를 완성한 것보다 이번 실습의 더 큰 수확이다.
