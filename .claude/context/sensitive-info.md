# 민감정보·안전

이 저장소는 **공개 커밋된다.** 지우는 것보다 애초에 안 적는 쪽이 안전하다.

- 회사 내부 프로세스명·조직명·시스템명·사내 용어는 적지 않는다. 필요하면 더 일반적인 표현으로 바꿔 적는다. 이 원칙은 지침 파일, 인터뷰 브리프, 설계 노트, 커밋 메시지 모두에 적용된다.
- **대화 내용을 파일로 남길 때**는 길이 컷 · 개행 접기 · 명백한 시크릿 접두사(`sk-`/`ghp_`/`AKIA`) 마스킹까지만 한다. 그 이상의 완전한 비식별화는 약속하지 않고, **한계를 문서에 명시한다.**
- 사용자 이메일 등 식별자는 작성자 식별 용도로만 쓴다. 외부 서비스의 요청 헤더·URL·본문에 넣지 않는다.
- 로컬에만 두는 파일(세션 기록, 훅 상태 파일)은 `.gitignore`로 잠근다.

근거: `.claude/context/README.md` "커밋 여부", `OS.md` 2026-09-07 `lastMessage` 마스킹 결정.
